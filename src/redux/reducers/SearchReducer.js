initialState = {
    firstName: '',
    middleName: '',
    lastName: ''
};

export const searchReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}